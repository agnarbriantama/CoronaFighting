extends CanvasLayer

func update_point(point_baru):
	$Control/Point.text = str(point_baru)
	
#func update_masker(mask_baru):
#	$Control2/Mask.text = str(mask_baru)
#func update_score(point_baru):
#	$Score/Label.text = "Your Score : " + str(point_baru)
	

